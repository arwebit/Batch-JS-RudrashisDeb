// Expression / Regular Function -- used mostly in JS

// const sayHello = function () {
//   console.log("Hello");
// };

// sayHello();

// Parameterized function

/*
const add = function (x, y) {
  return x + y;
};

console.log(add(2, 4));
*/

/* 
   -> it cannot be called before it declares. It is a good practise, it forces us to declare first and then use,
 which is  good for nice structural programming.

    -> functions are also value, that you can store in a variable, ex-expression function. 
*/

hello();

const hello = function () {
  console.log("Helllo");
};
